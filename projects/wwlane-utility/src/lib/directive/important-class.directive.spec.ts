import { ImportantClassDirective } from './important-class.directive';

describe('ImportantClassDirective', () => {
	it('should create an instance', () => {
		const directive = new ImportantClassDirective();
		expect(directive).toBeTruthy();
	});
});
