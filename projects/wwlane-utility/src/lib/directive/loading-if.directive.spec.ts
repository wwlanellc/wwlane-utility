import { LoadingIfDirective } from './loading-if.directive';

describe('LoadingIfDirective', () => {
	it('should create an instance', () => {
		const directive = new LoadingIfDirective();
		expect(directive).toBeTruthy();
	});
});
